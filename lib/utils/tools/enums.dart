import 'package:flutter/material.dart';
import 'package:get/get.dart';
part '../tools/extention.dart';

enum IRoutes { splash, launcher, detail, newsSource }

enum ITextStyles { demi, short, tall, venti }

enum IColors { athenaGrey, corduroy, fiord }

enum IDuration { demi, short }

enum LauncherTabs { home, favorite }

enum ApiCountryQuerry { tr, us }

enum IMeasures { screenOrientation }
